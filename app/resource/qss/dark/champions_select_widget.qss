/* Champions Select — Dark Theme */
#scrollArea {
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 8px;
    background-color: rgba(255, 255, 255, 0.04);
}

#scrollWidget {
    border: none;
    border-radius: 8px;
    background-color: transparent;
}